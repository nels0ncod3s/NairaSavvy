# NairaSavvy

The Next.js application is in [`nairasavvy/`](nairasavvy/). See the [setup, data review and deployment guide](nairasavvy/README.md).

```bash
cd nairasavvy
npm ci
npm run dev
```

GitHub Actions runs application checks from the repository root. Scout and Bayo agent definitions are managed separately from application changes.
